# 調査レポート: Cursor が `.claude` / `.codex` を読まないようにする

## 発見事項

### 結論（要約）

- **ワークスペース内**に置かれた `.claude/` や `.codex/` を、Cursor の **セマンティック検索・Tab・Agent・Inline Edit・@ 参照** から外すことは **可能** です。主な手段は `**.cursorignore`**（プロジェクトルート）と、Cursor 設定の **グローバル無視パターン** です。
- 一方で **「Cursor が一切そのパスを参照しない」** を完全に保証するものではありません。公式ドキュメントでも、無視しても **LLM の性質上 100% の保護は保証しない** と明記されています。
- **ターミナル経由や MCP ツール**で Agent が触るコードは、`.cursorignore` ではブロックできません（公式制限）。
- `**~/.claude` や `~/.codex`（ホーム直下）** は、通常は「開いているプロジェクトのコードベース索引」の対象外です。ただし **Claude Code 連携やサードパーティフック**など、製品が別経路で読む可能性は索引とは別問題です。

### 手段の整理


| 手段                        | 効く範囲           | メモ                                                                  |
| ------------------------- | -------------- | ------------------------------------------------------------------- |
| `.cursorignore`（リポジトリルート） | そのワークスペース内のパス  | `.gitignore` と同様の記法。`dist/` や `**/.claude/`** など                    |
| グローバル無視（Cursor Settings）  | 全プロジェクト共通のパターン | 例: `**/.env` など。`.claude` / `.codex` をパターンで追加可能                     |
| 階層的 `.cursorignore`       | 親ディレクトリを辿る     | 設定: Features → Editor → Hierarchical Cursor Ignore                  |
| `.gitignore`              | 索引のデフォルト無視に加算  | 本リポジトリでは `.gitignore` に `.claude/` あり（プロジェクト内 `.claude` は索引から外れやすい） |
| `.cursorindexingignore`   | **索引のみ**除外     | ドキュメント上「AI 機能にはアクセス可能のまま」— **読ませたくない用途には不向き**                       |


### 事実と推測の区別

- **事実**: [Cursor Ignore File 公式](https://cursor.com/docs/reference/ignore-file) に、`.cursorignore` の効果範囲・グローバル無視・`.cursorindexingignore` の差・ターミナル/MCP はブロック不可、が記載されている。
- **推測**: ホームの `~/.claude/agents` と `~/.cursor/agents` の **重複**については、本リポジトリの `scripts/deploy.sh` が警告を出しているが、これは「索引」ではなく **デプロイ/エージェント配置の重複**の話に近い。完全に「Cursor が ~/.claude を読まない」設定は、製品のエージェント解決ルールに依存するため、`.cursorignore` だけでは足りないケースがあり得る。

## 関連ファイル

- `/Users/y-asano/Environments/cursor/my-settings/.gitignore` — `.claude/` を無視
- `/Users/y-asano/Environments/cursor/my-settings/scripts/deploy.sh` — `~/.claude/agents` と Cursor 側の重複警告

## パターン・規約

- 無視パターンは **gitignore 形式**（`*`, `**`, `!` など）。親ディレクトリを除外するとネストの再包含に制限あり（公式の negation 注意書き参照）。
- プロジェクト内で `.claude` / `.codex` を隠したい場合の例（記載のみ、未適用）:
  - `.claude/`
  - `.codex/`
  - または `**/.claude/`** のように広げる（他ツール用にリポジトリ直下以外にもある場合）

## 依存関係

- Cursor のバージョン（グローバル無視 UI は 0.49+ で議論あり、フォーラム情報。最新は公式設定画面で確認推奨）。
- ワークスペースのルートがどこか（マルチルートやホームを開いている場合は索引範囲が変わる）。

## 制約・不変条件

- `.cursorignore` は **エディタ・索引・@ 参照経路**向け。シェルや MCP の経路は対象外。
- 「読まない」= **索引・コンテキスト自動取り込みを減らす** には有効。**別プロセスや別機能が明示的にパスを読む**挙動までは同一ファイルでは制御できない場合がある。

## 推奨事項

1. **リポジトリ内**の `.claude` / `.codex` を隠したい: ルートに `.cursorignore` に `/.claude/` と `/.codex/`（または既存の `.gitignore` との関係を確認しつつ）を追加する、または **グローバル無視**に `**/.claude/`** 等を追加する。
2. **索引だけ**減らし、必要なら手動で触りたい: `.cursorindexingignore` の用途を再確認（通常は「読ませない」目的には `.cursorignore` の方が近い）。
3. `**~/.claude` と `~/.cursor` の二重定義**が気になる: 索引ではなく **ファイルの実体を一箇所に寄せる**（シンボリックリンクやデプロイスクリプトの整理）が別軸の対策。
4. 次のステップとして要件を固めたら `**/plan`** で「ワークスペース限定か・全プロジェクトか・ホーム配下か」を切り分けて具体設定案にするとよい。

