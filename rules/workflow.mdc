---
description: "タスク種別の自動判定・オーケストレーション開始条件・エージェントチェーン定義"
alwaysApply: true
---

# Auto-Orchestration

以下の条件に該当するタスクを受けた場合、本ルールのワークフローに従って実行すること。

| タスク種別 | 判定条件 |
|-----------|---------|
| feature | 新機能追加、機能実装の要求 |
| bugfix | バグ修正、不具合対応の要求 |
| refactor | リファクタリング、構造改善の要求 |
| security | セキュリティ修正、脆弱性対応の要求 |
| debug | バグの根本原因が不明で体系的デバッグが必要な場合 |
| tdd | テスト駆動開発（RED→GREEN→REFACTOR）での実装要求 |

## 除外条件（直接実行してよい）

- 設定ファイルの値変更（JSON, YAML, TOML のパラメータ変更）
- ドキュメント・README の編集
- git 操作（コミット、ブランチ、マージ）
- 質問への回答・説明・調査

---

## エージェントチェーン

| タスク種別 | チェーン |
|-----------|---------|
| feature | `/planner` → `/qa-reviewer`(Contract検証) → [ユーザー確認] → `/generator` → `/qa-reviewer` → `/security-reviewer` |
| bugfix | `/researcher` → `/planner` → `/qa-reviewer`(Contract検証) → [ユーザー確認] → `/generator` → `/qa-reviewer` |
| refactor | `/architect` → `/planner` → `/qa-reviewer`(Contract検証) → [ユーザー確認] → `/generator` → `/qa-reviewer` |
| security | `/security-reviewer`(分析) → `/planner` → `/qa-reviewer`(Contract検証) → [ユーザー確認] → `/generator` → `/security-reviewer`(検証) → `/qa-reviewer` |
| debug | `/debugger` → `/verifier` → `/qa-reviewer` |
| tdd | `/planner` → `/qa-reviewer`(Contract検証) → [ユーザー確認] → `/generator`(TDDモード) → `/qa-reviewer` |

---

## サブエージェント実行規律（CRITICAL）

- チェーン内のサブエージェント起動は **デフォルト逐次実行** である
- 逐次チェーンでは、**1 ターン（1 回の応答）で起動してよいサブエージェントは 1 件** に限る。依存する後続サブエージェントを前段の結果受領前に起動してはならない
- 並列実行が許可されるのは `orchestration.mdc` で定義された明示的な並列モード（best-of-N / タスク分割）のみ
- サブエージェントが長時間かかっても待つこと。タイムアウトを理由にスキップしてはならない

---

## 共通ゲート（必須）

以下のゲートは全チェーンで省略不可。詳細・並列実行モード・ハンドオフ形式は **`orchestration.mdc` を Read ツールで読み込むこと**。

| ゲート | 内容 |
|--------|------|
| Gate 0 | Sprint Contract 交渉 — planner → qa-reviewer の検証基準レビュー |
| Gate 1 | ユーザー確認 — Contract への明示的な同意を得てから実装に進む |
| Gate 2 | lint / format / typecheck / test — 省略不可 |
| Gate 3 | PASS/FAIL 判定 — レビュアーごとに最大3回リトライ。3回 FAIL → 停止してユーザーに報告 |
