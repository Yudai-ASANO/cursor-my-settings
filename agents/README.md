# Agents

No personal agents are enabled by default.

Keep this directory for future optional agent definitions. Add files here only when a project-independent personal behavior is worth the extra context.
